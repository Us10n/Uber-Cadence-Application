allprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    group 'by.stas.uca'
    version '1.0'

    compileJava {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
}

